apiVersion: apps/v1
kind: Deployment
metadata:
  name: the-test-app
  namespace: 
  labels:
    app: the-test-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: the-test-app
  template:
    metadata:
      labels:
        app: the-test-app
    spec:
      containers:
      - name: the-test-app
        image: asia-south1-docker.pkg.dev/dev-zephyr-352206/the-test-app/the-test-app:latest
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3000"
        - name: OPENWEATHER_API_KEY
          valueFrom:
            secretKeyRef:
              name: app-secrets
              key: openweather-api-key
        resources:
          requests:
            memory: "64Mi"
            cpu: "50m"
          limits:
            memory: "128Mi"
            cpu: "100m"
---
apiVersion: v1
kind: Service
metadata:
  name: the-test-app-service
  namespace: 
spec:
  selector:
    app: the-test-app
  ports:
  - port: 80
    targetPort: 3000
  type: LoadBalancer
